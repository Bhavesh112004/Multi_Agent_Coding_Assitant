# Disable tracing to save CPU resources on your i5
OTEL_SDK_DISABLED=true

# Prevent CrewAI from asking for an OpenAI key (Used for local models via Ollama)
OPENAI_API_KEY=NA

# Optional: Set the local Ollama API base if using custom ports
# OPENAI_API_BASE='http://localhost:11434/v1'